version: '3.1'
services:
  postgres:
    image: postgres:12.0
    ports:
      - '5432:5432'
    volumes:
      - data:/var/lib/postgresql/data


    environment:
      - MYSQL_RANDOM_ROOT_PASSWORD=yes
      - MYSQL_DATABASE=app
      - MYSQL_USER=app
      - MYSQL_PASSWORD=password